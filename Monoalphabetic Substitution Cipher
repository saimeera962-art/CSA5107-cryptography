#include <stdio.h>

int main() {
    char p[100];
    char key[]="QWERTYUIOPASDFGHJKLZXCVBNM";
    int i;

    printf("Enter Plain Text: ");
    gets(p);

    for(i=0;p[i]!='\0';i++)
        if(p[i]>='A'&&p[i]<='Z')
            p[i]=key[p[i]-'A'];

    printf("Cipher Text: %s",p);

    return 0;
}
